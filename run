# to use in checkersPy as "sh run"
(cd static/ && npm run watch) &&
setsid firefox-developer --new-window localhost:5000 &
setsid atom . && export FLASK_ENV=app.py && export FLASK_DEBUG=1 && . venv/bin/activate && flask run
